# 🚀 API для лоадера VodkaClient

Документация для интеграции лоадера с сервером авторизации.

---

## 🔑 Endpoint 1: Проверка подписки (логин + пароль + HWID)

**URL:** `POST https://vodka-wf8h.onrender.com/api/launcher/check-subscription`

**Описание:** Проверяет логин, пароль, HWID и статус подписки пользователя.

### Запрос:
```json
{
  "username": "test",
  "password": "123456",
  "hwid": "A1B2C3D4E5F6G7H8I9J0K1L2M3N4O5P6"
}
```

### Ответ (успешно, есть подписка):
```json
{
  "success": true,
  "message": "Подписка активна",
  "has_subscription": true,
  "user": {
    "uid": 1,
    "username": "test",
    "created_at": "2024-01-01 12:00:00"
  },
  "subscription": {
    "type": "lifetime",
    "expires": "3361-01-01 00:00:00",
    "active": true
  }
}
```

### Ответ (успешно, НЕТ подписки):
```json
{
  "success": true,
  "message": "Подписка отсутствует или истекла",
  "has_subscription": false,
  "user": {
    "uid": 1,
    "username": "test",
    "created_at": "2024-01-01 12:00:00"
  },
  "subscription": {
    "type": null,
    "expires": null,
    "active": false
  }
}
```

### Ответ (ошибка):
```json
{
  "success": false,
  "message": "Неверный логин или пароль",
  "has_subscription": false
}
```

### Ответ (HWID не совпадает):
```json
{
  "success": false,
  "message": "Устройство не совпадает. Обратитесь в поддержку для сброса HWID",
  "has_subscription": false,
  "hwid_mismatch": true
}
```

---

## ⚡ Endpoint 2: Быстрая проверка по UID

**URL:** `GET https://vodka-wf8h.onrender.com/api/launcher/check-uid/{UID}`

**Описание:** Быстрая проверка подписки по UID (без пароля). Используй после первого входа.

### Пример запроса:
```
GET https://vodka-wf8h.onrender.com/api/launcher/check-uid/1
```

### Ответ (есть подписка):
```json
{
  "success": true,
  "has_subscription": true,
  "user": {
    "uid": 1,
    "username": "test"
  },
  "subscription": {
    "type": "lifetime",
    "expires": "3361-01-01 00:00:00",
    "active": true
  }
}
```

### Ответ (НЕТ подписки):
```json
{
  "success": true,
  "has_subscription": false,
  "user": {
    "uid": 1,
    "username": "test"
  },
  "subscription": {
    "type": null,
    "expires": null,
    "active": false
  }
}
```

---

## 💡 Логика работы в лоадере:

### Вариант 1: Первый запуск (нет сохраненных данных)
```
1. Пользователь вводит логин и пароль
2. Лоадер генерирует HWID устройства
3. Лоадер отправляет POST /api/launcher/check-subscription с логином, паролем и HWID
4. Если has_subscription = true:
   - Сервер сохраняет HWID (если первый вход)
   - Сохранить UID локально
   - Запустить игру
5. Если hwid_mismatch = true:
   - Показать сообщение "Устройство не совпадает. Обратитесь в поддержку"
6. Если has_subscription = false:
   - Показать сообщение "Купите подписку на сайте"
```

### Вариант 2: Повторный запуск (есть сохраненный UID)
```
1. Лоадер отправляет GET /api/launcher/check-uid/{UID}
2. Если has_subscription = true:
   - Запустить игру
3. Если has_subscription = false:
   - Показать форму входа снова
```

### HWID система:
```
- При первом входе HWID сохраняется в базе данных
- При последующих входах HWID проверяется
- Если HWID не совпадает - доступ запрещен
- Администратор может сбросить HWID через админ-панель
- После сброса пользователь может войти с нового устройства
```

---

## 📝 Типы подписок:

| Тип | Значение | Описание |
|-----|----------|----------|
| `lifetime` | Навсегда | Бессрочная подписка |
| `1month` | 30 дней | Месячная подписка |
| `6months` | 180 дней | Полугодовая подписка |
| `null` | - | Нет подписки |

---

## 🔒 Безопасность:

- Пароли хешируются с помощью bcrypt
- Используй HTTPS для всех запросов
- Не храни пароли в лоадере, только UID
- Проверяй `has_subscription` перед запуском игры
- HWID привязывает аккаунт к устройству
- HWID генерируется из CPU, материнской платы, BIOS и диска
- HWID хешируется SHA256 для безопасности
- Администратор может сбросить HWID при смене железа

---

## 🧪 Тестирование:

### С помощью curl:
```bash
# Проверка с логином, паролем и HWID
curl -X POST https://vodka-wf8h.onrender.com/api/launcher/check-subscription \
  -H "Content-Type: application/json" \
  -d '{"username":"test","password":"123456","hwid":"A1B2C3D4E5F6G7H8I9J0K1L2M3N4O5P6"}'

# Проверка по UID
curl https://vodka-wf8h.onrender.com/api/launcher/check-uid/1
```

### С помощью JavaScript:
```javascript
// Проверка с логином, паролем и HWID
const response = await fetch('https://vodka-wf8h.onrender.com/api/launcher/check-subscription', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    username: 'test',
    password: '123456',
    hwid: 'A1B2C3D4E5F6G7H8I9J0K1L2M3N4O5P6'
  })
});
const data = await response.json();

if (data.hwid_mismatch) {
  console.log('Устройство не совпадает!');
  // Показать сообщение об ошибке
} else if (data.has_subscription) {
  console.log('Подписка активна!');
  // Запустить игру
} else {
  console.log('Нет подписки');
  // Показать сообщение
}
```

---

## ❓ FAQ:

**Q: Что делать если сервер не отвечает?**  
A: Показать сообщение "Ошибка соединения с сервером. Проверьте интернет."

**Q: Как часто проверять подписку?**  
A: При каждом запуске лоадера. Можно добавить кеш на 5-10 минут.

**Q: Что если подписка истекла во время игры?**  
A: Проверяй подписку только при запуске. Во время игры не проверяй.

**Q: Что такое HWID?**  
A: Hardware ID - уникальный идентификатор устройства на основе железа (CPU, материнка, BIOS, диск).

**Q: Что если пользователь сменил железо?**  
A: Администратор может сбросить HWID через админ-панель, после чего пользователь сможет войти с нового устройства.

**Q: Можно ли обойти HWID?**  
A: HWID генерируется из реального железа и хешируется SHA256. Обход возможен только через виртуализацию, но это сложно.

**Q: Что если HWID не совпадает?**  
A: Показать сообщение "Устройство не совпадает. Обратитесь в поддержку для сброса HWID".

---

## 📞 Поддержка:

Telegram: @SmertnixVteste
