# 🔴 Инструкция по сбросу базы данных

## ⚠️ ВНИМАНИЕ! ОПАСНАЯ ОПЕРАЦИЯ!

Функция полного сброса базы данных удаляет **ВСЕ** данные без возможности восстановления.

---

## 🗑️ Что удаляется:

- ✅ **Все пользователи** - все аккаунты будут удалены
- ✅ **Все подписки** - все активные и неактивные подписки
- ✅ **Все ключи** - использованные и неиспользованные
- ✅ **Сброс UID** - счетчик UID начнется с 1

---

## 🔐 Защита от случайного удаления:

### 1. Тройное подтверждение:
- **Первое:** Диалоговое окно с предупреждением
- **Второе:** Ввод текста "УДАЛИТЬ ВСЕ"
- **Третье:** Ввод специального пароля

### 2. Специальный пароль:
```
RESET_ALL_DATA_2024
```

**ВАЖНО:** Храни этот пароль в секрете! Без него сброс невозможен.

---

## 📋 Как использовать:

### Шаг 1: Войти в админ-панель
```
URL: https://vodka-wf8h.onrender.com/admin.html
Логин: ServerVodki
Пароль: irairairA1
```

### Шаг 2: Прокрутить вниз
Найти секцию **"⚠️ ОПАСНАЯ ЗОНА"** в самом низу страницы.

### Шаг 3: Нажать кнопку
Нажать **"🗑️ СБРОСИТЬ ВСЮ БАЗУ ДАННЫХ"**

### Шаг 4: Подтвердить (3 раза)
1. Нажать **"OK"** в первом предупреждении
2. Ввести текст: **"УДАЛИТЬ ВСЕ"**
3. Ввести пароль: **"RESET_ALL_DATA_2024"**

### Шаг 5: Готово
База данных полностью очищена. Страница автоматически обновится.

---

## 🔧 Технические детали:

### API Endpoint:
```
POST /api/admin/reset-database
```

### Запрос:
```json
{
  "confirm_password": "RESET_ALL_DATA_2024"
}
```

### Ответ (успех):
```json
{
  "success": true,
  "message": "База данных полностью очищена. Все пользователи и ключи удалены."
}
```

### Ответ (ошибка):
```json
{
  "success": false,
  "message": "Неверный пароль подтверждения"
}
```

---

## 💡 Когда использовать:

✅ **Используй когда:**
- Нужно полностью очистить тестовые данные
- Начинаешь с чистого листа
- Переносишь проект на новый сервер

❌ **НЕ используй когда:**
- Есть реальные пользователи с подписками
- Нужно удалить только одного пользователя (используй кнопку "Удалить" в таблице)
- Не уверен в последствиях

---

## 🛡️ Безопасность:

### Что происходит на сервере:
```javascript
// 1. Проверка пароля
if (confirm_password !== 'RESET_ALL_DATA_2024') {
    return error;
}

// 2. Удаление всех пользователей
DELETE FROM users

// 3. Удаление всех ключей
DELETE FROM keys

// 4. Сброс автоинкремента
DELETE FROM sqlite_sequence WHERE name="users"
DELETE FROM sqlite_sequence WHERE name="keys"
```

### Логирование:
В консоли сервера появится запись:
```
⚠️ БАЗА ДАННЫХ ПОЛНОСТЬЮ ОЧИЩЕНА!
```

---

## 📝 Изменение пароля:

Если хочешь изменить пароль сброса, отредактируй `server.js`:

```javascript
// Найди эту строку:
if (confirm_password !== 'RESET_ALL_DATA_2024') {

// Замени на свой пароль:
if (confirm_password !== 'МОЙ_НОВЫЙ_ПАРОЛЬ') {
```

---

## ⚡ Быстрый сброс через API:

Если нужно сбросить БД через код или curl:

```bash
curl -X POST https://vodka-wf8h.onrender.com/api/admin/reset-database \
  -H "Content-Type: application/json" \
  -d '{"confirm_password":"RESET_ALL_DATA_2024"}'
```

---

## 🔄 После сброса:

1. Все таблицы пустые
2. Следующий зарегистрированный пользователь получит UID = 1
3. Все старые сессии станут недействительными
4. Лоадеры не смогут войти (нет пользователей)

---

## ❓ FAQ:

**Q: Можно ли восстановить данные после сброса?**  
A: Нет. Данные удаляются безвозвратно.

**Q: Что делать если забыл пароль?**  
A: Посмотри в `server.js` строку с `confirm_password`

**Q: Можно ли сбросить только пользователей, но оставить ключи?**  
A: Нет. Сброс удаляет всё. Используй кнопку "Удалить" для отдельных пользователей.

**Q: Сколько времени занимает сброс?**  
A: Мгновенно (< 1 секунды)

---

## 📞 Поддержка:

Если что-то пошло не так, свяжись с разработчиком.

**Telegram:** @SmertnixVteste
